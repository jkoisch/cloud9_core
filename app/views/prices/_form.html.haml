= form_for @cloud9_price do |f|
  - if @cloud9_price.errors.any?
    #error_explanation
      %h2= "#{pluralize(@cloud9_price.errors.count, "error")} prohibited this cloud9_price from being saved:"
      %ul
        - @cloud9_price.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
