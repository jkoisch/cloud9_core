= form_for @cloud9_measurement do |f|
  - if @cloud9_measurement.errors.any?
    #error_explanation
      %h2= "#{pluralize(@cloud9_measurement.errors.count, "error")} prohibited this cloud9_measurement from being saved:"
      %ul
        - @cloud9_measurement.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
