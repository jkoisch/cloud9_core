= form_for @cloud9_product do |f|
  - if @cloud9_product.errors.any?
    #error_explanation
      %h2= "#{pluralize(@cloud9_product.errors.count, "error")} prohibited this cloud9_product from being saved:"
      %ul
        - @cloud9_product.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
