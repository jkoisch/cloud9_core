= form_for @cloud9_user do |f|
  - if @cloud9_user.errors.any?
    #error_explanation
      %h2= "#{pluralize(@cloud9_user.errors.count, "error")} prohibited this cloud9_user from being saved:"
      %ul
        - @cloud9_user.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
