= form_for @cloud9_customer do |f|
  - if @cloud9_customer.errors.any?
    #error_explanation
      %h2= "#{pluralize(@cloud9_customer.errors.count, "error")} prohibited this cloud9_customer from being saved:"
      %ul
        - @cloud9_customer.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
